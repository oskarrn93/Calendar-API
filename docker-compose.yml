version: '3'
services:
  api:
    image: node:13.6
    network_mode: 'host'
    env_file:
      - config.env
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/code/
    ports:
      - '8001:8001'
    command: bash -c "cd /code && npm install && npm run dev"
